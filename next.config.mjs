/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['pps.whatsapp.net', 'gpt-files.com', 'tempstorage.download'],
  },
};

export default nextConfig;
